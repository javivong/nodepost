{
  "swagger": "2.0",
  "info": {
    "version": "1.0.0",
    "title": "Swagger API for Players DB",
    "description": "Swagger API for Players DB"
  },
  "host": "localhost:3000",
  "basePath": "/api/v1",
  "tags": [
    {
      "name": "Players",
      "description": "Players in the system"
    }
  ],
  "schemes": [
    "https"
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/" : {
      "get" : {

      }
    },
    "/ranking": {
      "get": {
        "tags": [
          "Players"
        ],
        "description": "Get the number and the ranking of the players by score."
      }
    },
    "/players/{alias}": {
      "parameters": [
        {
          "name": "alias",
          "in": "path",
          "required": true,
          "description": "Alias of the player that we want to accés",
          "type": "string"
        }
      ],
      "get": {
        "tags": [
          "Players"
        ],
        "summary": "Get player by alias",
        "responses": {
          "200": {
            "description": "Player is found",
            "schema": {
              "$ref": "#/definitions/Response"
            }
          }
        }
      },
      "put": {
        "summary": "Update player by alias",
        "tags": [
          "Players"
        ],
        "parameters": [
          {
            "name": "user",
            "in": "body",
            "description": "User with new values of properties",
            "schema": {
              "$ref": "#/definitions/Player"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "User is updated",
            "schema": {
              "$ref": "#/definitions/Player"
            }
          }
        }
      },
      "post": {
        "summary": "Create player",
        "tags": [
          "Players"
        ],
        "parameters": [
          {
            "name": "user",
            "in": "body",
            "description": "User with new values of properties",
            "schema": {
              "$ref": "#/definitions/Player"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "User is updated",
            "schema": {
              "$ref": "#/definitions/Player"
            }
          }
        }
      }
    }
  },
  "definitions": {
    "Player": {
      "required": [
        "alias",
        "name",
        "surname",
        "score"
      ],
      "properties": {
        "position": {
          "type": "string"
        },
        "alias": {
          "type": "string",
          "uniqueItems": true
        },
        "name": {
          "type": "string"
        },
        "surname": {
          "type": "string"
        },
        "score": {
          "type": "string"
        },
        "created": {
          "type": "string"
        }
      }
    },
    "Players": {
      "type": "array",
      "$ref": "#/definitions/Player"
    },
    "Code": {
      "properties": {
        "code": {
          "type": "integer"
        },
        "error": {
          "type": "boolean"
        },
        "message": {
          "type": "string"
        }
      }
    },
    "Response": {
      "properties": {
        "code": {
          "type": "integer"
        },
        "error": {
          "type": "boolean"
        },
        "message": {
          "type": "string"
        },
        "player":{
          "type":"object",
          "$ref": "#/definitions/Player",
          "additionalProperties": {"type": "string"}
        }
      }
    }
  }
}